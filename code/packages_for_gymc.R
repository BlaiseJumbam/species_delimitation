# install.packages(c("ape", "paran", "rncl"))
# install.packages("splits", repos = "http://R-Forge.R-project.org")
# install.packages("tidyverse")

library(rncl) # used to read in trees in nexus format

library(splits) # gmyc analysis

library(tidyverse) # data manipyaltion and plotting